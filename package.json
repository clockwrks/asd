{
  "name": "simpleplugin",
  "author": "yourname",
  "version": "0.0.1",
  "main": "dist/SimplePlugin.js",
  "scripts": {
  "build": "esbuild src/index.ts --bundle --platform=neutral --format=cjs --outfile=dist/SimplePlugin.js --external:enmity/*"
}
,
  "dependencies": {
    "enmity": "*"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
